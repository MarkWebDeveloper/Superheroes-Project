// import { email, password, route, router } from "./LoginForm.vue";

// export function login() {

// const matchingUser = users.find(user => user.username.value === email && user.password.value === password);

// if (matchingUser) {

// matchingUser.isAuthenticated = true;
// const redirectPath = route.query.redirect || '/favourites';
// router.push(redirectPath);
// } else {
// console.log("Credenciales incorrectas. Por favor, inténtalo de nuevo.");
// }
// }
